import unittest
from acme import Product
from acme_report import generate_products, adj, noun

class AcmeReportTest():

    def test_default_num_products()
    df = (products)
        processor = DataProcessor(df)
        self.assertEqual(list(processor.df.columns), ['products'])

        processor.add_state_names()

        
        self.assertEqual(list(processor.df.columns), ["products"])





    def test_legal_names()
        



    if __name__ == '__main__':
    unittest.main()